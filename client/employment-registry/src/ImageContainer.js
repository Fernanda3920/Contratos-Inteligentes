/*import React from 'react';
import PropTypes from 'prop-types';
import './App.css'; // Puedes agregar estilos personalizados aquí

const ImageContainer = ({ src, alt, style, className }) => {
  return (
    <div className={`image-container ${className}`} style={style}>
      <img src={src} alt={alt} className="image" />
      <div className='container'> 
        <h1> ALTA DE REGISTROS </h1>
        <p>
          Gereración de contrato digital.
        </p>

        <button>Dar de alta un nuevo empleado</button>  
        <button>Recuperar datos de un empleado</button>  
      </div>
    </div>
    
  );
};


export default ImageContainer;*/
